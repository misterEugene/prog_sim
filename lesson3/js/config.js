// ============================================================
// Урок 3 «Аналитик данных и создатель ИИ» — KNN-симулятор.
// config.js — все константы в одном месте (цвета, размеры, параметры).
// Классический скрипт (без ES-модулей), глобальный объект CONFIG.
// ============================================================

const CONFIG = {
  // Поле для рисования
  CANVAS_SIZE: 600,        // canvas 600x600 px
  HEATMAP_GRID: 20,        // тепловая карта 20x20 ячеек
  GRID_LINES: 20,          // линий сетки

  // Алгоритм KNN
  DEFAULT_K: 5,
  MIN_K: 1,
  MAX_K: 15,

  // Точки
  POINT_RADIUS: 7,
  TEST_RADIUS: 9,
  GLOW_BLUR: 12,           // свечение точек (shadowBlur)

  // Стартовый датасет
  STARTER_PER_CLASS: 15,   // по 15 точек каждого класса (всего 30)

  // Секретный тест
  TEST_COUNT: 5,           // 5 серых точек
  TEST_MARGIN: 60,         // отступ тестовых точек от краёв

  // Прозрачность тепловой карты
  HEAT_ALPHA_MIN: 0.07,    // у границы (уверенность ~50%)
  HEAT_ALPHA_MAX: 0.55,    // в глубине класса (уверенность ~100%)

  // Цвета (тёмная тема, неон)
  COLORS: {
    blue: '#00d4ff',       // неоново-синий класс
    red: '#ff4757',        // неоново-красный класс
    blueRgb: [0, 212, 255],
    redRgb: [255, 71, 87],
    bg: '#12151e',
    grid: 'rgba(255, 255, 255, 0.05)',
    test: '#9aa0b5',       // нейтральный серый для тестовых точек
    pointStroke: '#0b0d14'
  }
};
