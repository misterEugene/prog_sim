# Переработка урока 2 «Этичный хакер»: дробление шагов + пункты действия

> Дата планирования: 2026-06-23. Запрос пользователя: урок 2 нужно **сильнее
> раздробить на шаги** (большие шаги-«дыры» разбить на мелкие), и в **каждом шаге**
> добавить **пункты действия** — короткий чек-лист конкретных действий.
>
> Аудитория: дети 12–16 лет без опыта. Цель — чтобы ни один шаг не был «простынёй»,
> а ребёнок всегда видел короткий список «что сделать сейчас».

## СТАТУС: РЕАЛИЗОВАНО (2026-06-23)

Сделано: 9 шагов → **20 шагов**, в каждом — блок «📋 Пункты действия». Админ-читы
(`stepFixes`) перестроены под новые индексы. Подробности — в §8 ниже.

## 0. ГЛАВНОЕ открытие: `build_lesson2.py` УСТАРЕЛ — правили JS напрямую

Исходный план предполагал правку через `scripts/build_lesson2.py`. **Это оказалось
неверно:** закоммиченный `js/lesson-data-security.js` был на ~550 строк богаче
скрипта (шаги давно правят прямо в JS: хэширование паролей в Дыре №3, доп.
атаки/починки в Дыре №1, админ-читы `stepFixes`). Запуск `build_lesson2.py` стёр бы
половину урока (120КБ → 61КБ). Поэтому:

1. **Источник истины для шагов урока 2 — сам `js/lesson-data-security.js`**, правим
   его напрямую. `build_lesson2.py` **не запускаем** (он затрёт шаги).
2. У скрипта жива только генерация `VULN_*` (строки 1–725). Если урок 1 изменится и
   нужно обновить вшитый сайт — менять ТОЛЬКО блок до `const lesson = {`, не трогая
   шаги/`stepFixes`. См. PITFALLS «`build_lesson2.py` УСТАРЕЛ для ШАГОВ».
3. `VULN_*` + intro/outro/hint (строки 1–725) остались **байт-в-байт** (проверено
   `head -725` diff пуст).
4. После правок: `node --check js/lesson-data-security.js` — OK.

Поля одного шага: `title`, `time`, `goalMd`, `actionMd`, `taskMd`, `hintMd`,
`doneMd`. Все шаги — `manual:true` (кнопка «✓ Я выполнил этот шаг»). Прогресс
«N из M» считается по числу шагов автоматически — увеличение числа шагов ничего не
ломает в движке (`lesson-render.js` не трогаем).

**Админ-читы.** В конце файла — `lesson.adminFixMode` + `lesson.stepFixes` (мап
индекс шага → правки кода; движок «🗝 Пройти по шаг N» в `js/dev-cheat.js`). При
дроблении шагов этот массив **обязательно перестраивать** под новые индексы
(см. §8 и PITFALLS).

## 1. Что сейчас (9 шагов)

| # | Шаг | Время | Проблема |
|---|-----|-------|----------|
| 0 | 🧰 Инструменты этичного хакера | 12 мин | 6 крупных пунктов (магазин, консоль, F12, 3 вкладки) — терпимо, но можно разбить на 2 |
| 1 | 💉 Дыра №1: XSS в комментариях | **35 мин** | **Самый тяжёлый.** Внутри ~4 цикла 🔴→🟢→✅: комментарий, кража данных, stored XSS у других, починка `renderComments`, XSS в имени, починка `renderUserBox`+приветствия, скрытая дыра в имени автора, починка `author` |
| 2 | 🔑 Дыра №2: пароль в коде + обход входа | 25 мин | 🔴 найти пароль + 🔴 обойти вход + 🟢 починить — 3 в одном |
| 3 | 🗄️ Дыра №3: пароли открытым текстом | — | 🔴 прочитать + 🟢 защита — 2 в одном |
| 4 | 🔓 Дыра №4: слабый пароль / брутфорс | — | 🔴 перебор в консоли + 🟢 защита — 2 в одном |
| 5 | 🛂 Дыра №5: фальшивый «админ» | — | 🔴 подмена `is_admin` + 🟢 починка — 2 в одном |
| 6 | 🔗 Дыра №6: отражённый XSS из ссылки | — | 🔴 доставка payload + 🟢 починка — 2 в одном |
| 7 | 🚩 Бонус: спрятанные флаги | — | 3 флага в одном — ок, оставить |
| 8 | 🏁 Финал | — | ок |

## 2. Принцип дробления

**Один шаг = один смысловой блок ≤ ~10–12 минут.** Базовая единица урока 2 — цикл
🔴 Найди/сломай → 🟢 Почини → ✅ Проверь. Дробим так:

- **🔴 «Найди/сломай» — отдельный шаг.** Ребёнок сперва своими глазами видит
  опасность (атака срабатывает), нажимает «✓ Я выполнил» и идёт чинить.
- **🟢 «Почини» + ✅ «Проверь» — следующий шаг** (починка маленькая без проверки
  бессмысленна, поэтому держим их вместе: поправил код → ▶ Запустить → повтори
  атаку → убедись, что не работает).
- Если внутри одной «дыры» несколько РАЗНЫХ мест/атак (как в Дыре №1) — каждое
  место становится своей парой шагов 🔴/🟢.

Итог ≈ **18–20 шагов** вместо 9 (см. таблицу в §4). Прогресс «N из 20».

## 3. Формат «пунктов действия» (в КАЖДОМ шаге)

В начало `taskMd` каждого шага добавляем стандартный блок **«📋 Пункты действия»** —
короткий нумерованный чек-лист (3–6 пунктов) из коротких повелительных фраз. Это
«карта шага»: ребёнок видит весь маршрут целиком, прежде чем читать подробности.

Под чек-листом — прежний подробный разбор «клик за кликом» (он остаётся, просто
короче на шаг, потому что шаги мельче). Структура `taskMd`:

```
📋 **Пункты действия** (что сделать в этом шаге):
1. Зарегистрируйся на сайте.
2. Войди под своим логином.
3. Открой комментарии у любого товара.
4. Вставь в комментарий «ловушку» и отправь.
5. Убедись, что выскочило окошко — это XSS.

---

🔧 **Подробно, по шагам:**
… (существующий текст «клик за кликом») …
```

**Реализация формата — без правок движка/CSS:** блок «📋 Пункты действия» — это
обычный markdown (жирный заголовок + нумерованный список), который уже умеет
рендерить `js/markdown.js`. Новые поля шага НЕ вводим (не трогаем `step_js`,
`lesson-render.js`, CSS) — всё живёт внутри `taskMd`.

> Альтернатива (НЕ берём пока): отдельное поле `actionsMd` + свой стиль
> `.action-items` в `css/markdown.css`. Красивее визуально, но требует правок ядра
> (общего для всех уроков) и `step_js`. Если по итогам ручной проверки захочется
> выделить чек-лист рамкой/фоном — вернёмся к этому отдельной задачей.

## 4. Целевая раскадровка (≈19 шагов)

| Новый # | Шаг | Из старого | Тип |
|---------|-----|-----------|-----|
| 0 | 🧰 Осмотрись: магазин, ▶ Запустить, прогулка, консоль | S0 (п.1–3) | подготовка |
| 1 | 🧰 Открой DevTools: F12 и три вкладки | S0 (п.4–6) | подготовка |
| 2 | 💉 XSS в комментарии: 🔴 сломай (alert + кража currentUser + stored у других) | S1 ч.1 | 🔴 |
| 3 | 💉 XSS в комментарии: 🟢 почини `renderComments` + ✅ проверь | S1 ч.2 | 🟢 |
| 4 | 💉 XSS в имени пользователя: 🔴 сломай (вредный логин) | S1 ч.3 | 🔴 |
| 5 | 💉 XSS в имени: 🟢 почини `renderUserBox`+приветствие + ✅ | S1 ч.4 | 🟢 |
| 6 | 💉 Скрытая дыра — имя автора: 🔴 сломай (обычный коммент, вредный логин) | S1 ч.5 | 🔴 |
| 7 | 💉 Имя автора: 🟢 почини `author.innerHTML` + ✅ | S1 ч.6 | 🟢 |
| 8 | 🔑 Пароль в коде + HTML-комментарии: 🔴 найди и войди бэкдором | S2 ч.1 | 🔴 |
| 9 | 🔑 Обход входа через `localStorage.isLoggedIn`: 🔴 | S2 ч.2 | 🔴 |
| 10 | 🔑 Убери секрет и бэкдор: 🟢 + ✅ | S2 ч.3 | 🟢 |
| 11 | 🗄️ Пароли открытым текстом: 🔴 прочитай чужой из Local Storage | S3 ч.1 | 🔴 |
| 12 | 🗄️ Защита + что такое хэш: 🟢 + ✅ | S3 ч.2 | 🟢 |
| 13 | 🔓 Брутфорс слабого пароля: 🔴 перебор в консоли | S4 ч.1 | 🔴 |
| 14 | 🔓 Защита от перебора (длина/попытки): 🟢 + ✅ | S4 ч.2 | 🟢 |
| 15 | 🛂 Фальшивый «админ»: 🔴 подмена `is_admin` | S5 ч.1 | 🔴 |
| 16 | 🛂 Убери секрет с клиента: 🟢 + ✅ | S5 ч.2 | 🟢 |
| 17 | 🔗 Отражённый XSS из `#welcome=`: 🔴 доставка payload | S6 ч.1 | 🔴 |
| 18 | 🔗 Почини `showWelcome`: 🟢 + ✅ | S6 ч.2 | 🟢 |
| 19 | 🚩 Бонус: собери 3 флага | S7 | бонус |
| 20 | 🏁 Финал | S8 | финал |

> Числа в таблице — ориентир. Шаги 2 и 6 (🔴) можно оставить чуть крупнее, если
> разрезать выйдет искусственно. Главное — выдержать принцип «один шаг = одно
> действие/один цикл» и чек-лист в каждом.

Для каждого нового шага: `goalMd` — что и зачем (1–2 фразы); `actionMd` — одна
строка «что делаем»; `taskMd` — **«📋 Пункты действия»** + подробный разбор;
`hintMd` — подсказка; `doneMd` — критерий «✅ Готово, когда…»; `time` —
пересчитать (сумма по старой «дыре» делится между её под-шагами).

## 5. Что НЕ трогаем

- Вшитый сайт `VULN_HTML/CSS/JS` (генерируется из урока 1) — без изменений.
- Намеренные уязвимости и CTF-флаги — те же, меняется только подача (дробление).
- Движок шагов (`js/lesson-render.js`), хранилище, CSS — без изменений.
- Урок 1 и урок 3 — не трогаем (запрос только про урок 2).

## 6. Принятые решения

- **Глубина дробления:** взято **20 шагов** (🔴-«сломай» и 🟢-«почини» — раздельные
  шаги; Дыра №1 → 6 шагов; ориентация → 2). ✅ Проверь не выносили в отдельный шаг —
  он живёт вместе с 🟢-починкой.
- **Формат пунктов действия:** markdown-чек-лист «📋 Пункты действия» в начале
  `taskMd` (без правок ядра). Отдельное поле/CSS не вводили.

## 7. Как сделано (реализация 2026-06-23)

- Дробление выполнено генератором `scratchpad/split_lesson2.py`: он **парсит**
  текущие шаги из JS (template-литералы с разэкранированием `` \` ``/`\${`/`\\`),
  режет большие `taskMd` по разделителям `\n\n---\n\n` на 🔴/🟢-части, добавляет
  блок «📋 Пункты действия» и пересобирает ТОЛЬКО массив `steps:` (VULN_* и хвост —
  дословно). Прозу шагов не перепечатывали — перенос точь-в-точь (ниже риск опечаток
  в коде, который копируют дети).
- Заголовки шагов получили подписи `— 🔴 сломай` / `— 🟢 почини` и метки
  `Дыра №1а/1б/1в`.
- `lesson.stepFixes` перестроен под 20 индексов (см. PITFALLS). Дыра №1 разбита на
  3 починки: шаг 3 ставит промежуточный `FIX_RENDER_COMMENTS_TEXT` (текст безопасен,
  имя автора ещё `innerHTML`), шаг 5 — `renderUserBox` + приветствие, шаг 7 —
  `replaceStr` `author.innerHTML`→`textContent`.

## 8. Проверки (Node; браузера в окружении нет)

- [x] `node --check js/lesson-data-security.js` — OK.
- [x] `head -725` нового и закоммиченного файла идентичны (VULN_* + intro/outro/hint
  не тронуты).
- [x] 20 шагов; в каждом ровно 1 блок «📋 Пункты действия»; все ` ``` ` парные.
- [x] Симуляция админ-читов: правки применены кумулятивно до каждого шага — **все
  якоря находятся**, финальный «решённый» сайт закрывает все дыры (renderComments/
  имя/showWelcome безопасны, пароль и секрет убраны, хэширование и проверка длины
  на месте, админ-панель пуста).
- [ ] **Ручная проверка в браузере** (`index2.html`): прогресс «N из 20», линейная
  разблокировка, чек-лист виден в каждом шаге, атаки/починки совпадают с текстом,
  админ-кнопка «🗝 Пройти по шаг N» (`#mega-admin`) даёт верное состояние на каждом
  шаге.
